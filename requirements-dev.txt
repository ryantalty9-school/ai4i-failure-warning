# Just what the tests need - the heavy tools (MLflow, DVC, Great Expectations,
# XGBoost, Streamlit) are not required to run the test suite in CI.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
pytest>=8.0
